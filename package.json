{
  "name": "f3-vegetables",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/prepare-dist.mjs && esbuild src/main.jsx --bundle --outdir=dist/assets --entry-names=app --asset-names=[name] --loader:.png=file --loader:.jpg=file --loader:.jpeg=file --loader:.webp=file --loader:.svg=file --format=esm --minify && node scripts/write-html.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
